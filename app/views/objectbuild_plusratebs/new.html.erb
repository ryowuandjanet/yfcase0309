<h2>New</h2>
<%= current_user.first_name %><%= current_user.last_name %>
<%= form_for @plusrateb , :url => yfcase_objectbuild_plusratebs_path( @yfcase,@objectbuild ) do |f| %>
<div class="form-group">
    <%= f.label :plusb %>
    <%= f.number_field :plusb, in: 0..10, :step => 0.01, :required => true , :class => "form-control" %>
    <%= f.hidden_field :personb, :value => "#{current_user.first_name}#{current_user.last_name}" %>
</div>

<div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
</div>
<% end %>
<%= link_to "back", yfcase_path(@yfcase) %>